# Pinned OpenClaw artifacts

Generated from OpenClaw **2026.6.1** (`2e08f0f`) via prototype `dragonclaw-build extract-schema`.

Source: `../.oc-source` (git clone of `openclaw/openclaw` tag `v2026.6.1`).

Regenerate after OC pin bumps:

```bash
cd ~/dragonclaw_project
.venv/bin/dragonclaw-build extract-schema ~/dragonclaw/.oc-source \
  --output ~/dragonclaw/artifacts/schema.json \
  --config-output ~/dragonclaw/artifacts/config_surface.json \
  --oc-version <new-version>
```
